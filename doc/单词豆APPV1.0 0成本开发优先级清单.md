# 单词豆V1.0 0成本开发优先级清单

文档版本：V1.0

编写日期：2025年12月30日

适配文档：《单词豆（儿童英语单词连连看小游戏）程序概要设计文档（V1.0）》

核心原则：全程采用免费技术栈/工具，优先落地核心功能，规避付费服务与冗余开发，控制开发周期与成本（0成本落地）。

# 一、P0级：核心必做任务（上线必备，0成本落地）

目标：实现核心功能闭环，满足“自定义单词管理+连连看游戏+基础进度跟踪”核心需求，确保多端适配流畅、本地存储稳定。

## 1. 技术栈搭建（0成本，1-2天）

1. **开发环境部署**：安装HBuilder X（免费）、VS Code（免费），配置Uni-app（Vue3+Vite）免费开发环境，无需付费插件。

2. **免费工具集成**：集成uView UI免费组件库、Day.js免费日期工具、原生JS表单校验（替代付费Validator）、js-base64免费加密辅助工具。

3. **版本控制搭建**：使用Git（免费）+ GitHub/GitLab（免费仓库），实现代码管理与协作，无需付费版本控制工具。

## 2. 核心模块开发（0成本，5-7天）

### 2.1 自定义单词管理模块

1. 实现单条单词录入（单词+音标+释义，支持免费图库素材导入配图，如Pexels无版权图片）。

2. 实现文本批量导入（支持.txt格式解析，按“单词,音标,释义”拆分，错误数据提示，无付费解析工具）。

3. 实现单词集创建/编辑/删除（内置3类基础单词集：动物/水果/颜色，本地存储，支持排序）。

4. 简化分享功能（生成微信可识别链接/二维码，利用Uni-app免费API实现，无需付费分享服务）。

### 2.2 连连看核心游戏模块

1. 实现单词集选择与关卡生成（3种难度布局：3×4/4×5/5×6，原生JS洗牌算法随机排列卡片）。

2. 实现核心匹配玩法（原生JS键值对匹配算法，点击响应≤0.3秒，消除动画用Lottie免费模板）。

3. 实现基础提示功能（每局限3次，高亮可匹配卡片，无付费交互组件）。

4. 实现简单反馈与奖励（鼓励文字弹窗、勋章本地存储，无付费奖励系统）。

### 2.3 基础进度跟踪模块

1. 实现学习记录统计（时长、正确率、错误单词，按日期分组存储，本地展示）。

2. 实现单词掌握率计算（正确匹配≥3次标记为已掌握，实时更新展示）。

3. 实现简易学习日报（文本+数据格式，每日24点自动生成，支持截图保存，无付费排版工具）。

### 2.4 基础设置模块

1. 实现本地数据加密存储（AES-128免费加密算法，无付费加密服务）。

2. 实现学习时长限制（默认15分钟，超时锁定，本地计时，无付费计时工具）。

3. 实现手动数据备份/清除（本地文本导出备份，清除前弹窗确认，无云端备份）。

4. 实现音效/动画开关（集成免费开源音效，支持开启/关闭，无付费音效库）。

## 3. 多端适配开发（0成本，2-3天）

1. 手机端：适配Android 8.0+/iOS 12.0+，优化触控按钮（≥44px×44px），竖屏布局。

2. 小程序端：优化包体积（≤2MB），免费分包加载，首屏加载≤2秒，适配微信小程序最新版。

3. 电脑端：适配Chrome/Edge免费浏览器，支持鼠标点击与键盘快捷键，分辨率自适应。

4. 平板端：复用手机端代码，条件编译适配横屏布局，无需额外开发。

## 4. 测试与优化（0成本，2-3天）

1. 功能测试：使用Uni-app模拟器（免费）、Chrome DevTools（免费），覆盖核心场景。

2. 性能优化：压缩图片/音效资源，清理冗余代码，确保首屏加载≤3秒、关卡加载≤1秒。

3. 异常处理：全局添加原生JS try-catch捕获，本地记录错误日志，避免闪退。

4. 兼容性测试：在主流免费设备/浏览器中测试，修复适配问题，无需付费测试设备。

# 二、P1级：次要优化任务（上线后迭代，0成本）

1. 优化单词录入体验：增加单词发音（系统免费合成，无付费发音接口）、输入联想提示（原生JS实现）。

2. 优化游戏互动：增加连续匹配奖励（额外鼓励弹窗）、错误单词二次复习环节。

3. 优化进度展示：增加学习周统计（文本格式，展示近7天学习数据）。

4. 优化多端同步：支持手动导入/导出数据（文本格式），实现多端数据迁移（无自动同步）。

# 三、P2级：后期扩展任务（V1.0稳定后，0成本）

1. 扩展单词集：新增教材同步（手动导入教材单词文本，无付费教材库）。

2. 优化奖励体系：增加道具解锁（累计勋章解锁提示道具，本地管理）。

3. 支持更多导入格式：新增Excel格式单词导入（使用免费开源Excel解析工具）。

4. 扩展适配：适配支付宝小程序（免费开发，复用核心代码）。

# 四、0成本开发核心控制点

1. **工具管控**：全程使用免费工具（开发/测试/设计），拒绝任何付费插件/服务。

2. **资源管控**：图片/音效/动画均选用无版权免费素材，规避版权风险与付费成本。

3. **技术管控**：核心算法/功能用原生JS实现，无需引入付费框架/库，减少依赖。

4. **开发管控**：复用核心代码跨端部署，简化非核心功能，避免冗余开发。

5. **运维管控**：无云端服务，本地存储为主，规避服务器租赁、带宽等运维成本。

# 五、落地时间规划（总计12-17天，0成本）

|阶段|任务内容|耗时（天）|核心目标|
|---|---|---|---|
|1|技术栈搭建+环境配置|1-2|完成免费开发环境部署，确保工具可用|
|2|核心模块开发|5-7|实现核心功能闭环，满足基础使用需求|
|3|多端适配开发|2-3|实现多端流畅适配，无明显适配问题|
|4|测试与优化|2-3|修复功能/性能问题，确保稳定上线|
|5|上线与迭代|1-2|免费渠道上线（微信小程序/应用市场），启动P1级优化|
> （注：文档部分内容可能由 AI 生成）